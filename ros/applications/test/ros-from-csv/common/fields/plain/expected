#python

# only run these tests if we have rospy installed
try:
    import rospy

    expect fields == "header/seq,header/stamp,header/frame_id,status/status,status/service,latitude,longitude,altitude,position_covariance,position_covariance_type"

except ImportError:
    pass
